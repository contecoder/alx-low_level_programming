#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_strcpy- the function that change value
 *@dest: the pointer of n
 *@src: the seconde value
 *
 *Return: char*
 */
char *_strcpy(char *dest, char *src)
{
  return (strcpy(dest, src));
}
