#include"main.h"
#include<stdio.h>
/**
 *_puts- the function that change value
 *@str: the pointer of n
 *
 */
void _puts(char *str)
{
int i = 0;
while (*(str + i) != '\0')
{
putchar(*(str + i));
i++;
}
putchar('\n');

}
