#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_strstr- check the
 *@haystack: the first
 *@needle: seconde
 *Return: Always 0.
 */
char *_strstr(char *haystack, char *needle)
{
return (strstr(haystack, needle));
}
