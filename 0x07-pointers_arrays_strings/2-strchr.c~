#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_memcpy- check the
 *@dest: the first
 *@src: the seconde
 *@n: the third value
 *Return: Always 0.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
return (memcpy(dest, src, n));
}
