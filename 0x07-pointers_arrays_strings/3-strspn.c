#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_strspn- check the
 *@s: the first
 *@accept: seconde
 *Return: Always 0.
 */
unsigned int _strspn(char *s, char *accept)
{
return (strspn(s, accept));
}
