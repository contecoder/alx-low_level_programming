#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_memset- check the
 *@s: the first
 *@b: the seconde
 *@n: the third value
 *Return: Always 0.
 */
char *_memset(char *s, char b, unsigned int n)
{
return (memset(s, b, n));
}
