#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_strpbrk- check the
 *@s: the first
 *@accept: seconde
 *Return: Always 0.
 */
char *_strpbrk(char *s, char *accept)
{
return (strpbrk(s, accept));
}
