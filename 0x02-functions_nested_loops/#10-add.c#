#include"main.h"
/**
 *add - that function print alphabet 10 times
 *@n: first arg
 *@m: second arg
 *
 *return: somme the result of somme
 */
int add(int n , int m);
int add(int n , int m)
{
int somme ;
somme = n + m ; 
return (somme);
}
