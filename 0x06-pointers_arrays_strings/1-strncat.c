#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_strncat- check the
 *@dest: the first
 *@src: the seconde
 *@n: taille
 *Return: Always 0.
 */
char *_strncat(char *dest, char *src, int n)
{
return (strncat(dest, src, n));
}
