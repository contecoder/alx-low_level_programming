#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_strcmp- check the
 *@s1: the first
 *@s2: the seconde
 *
 *Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
return (strcmp(s1, s2));
}
