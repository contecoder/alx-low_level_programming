#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_strncpy- check the
 *@dest: the first
 *@src: the seconde
 *@n: taille
 *Return: Always 0.
 */
char *_strncpy(char *dest, char *src, int n)
{
return (strncpy(dest, src, n));
}
