#include"main.h"
#include<stdio.h>
#include<string.h>
/**
 *_strcat- check the
 *@dest: the first
 *@src: the seconde
 *Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
return (strcat(dest, src));
}
